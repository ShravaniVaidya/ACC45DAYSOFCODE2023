#include <iostream>
using namespace std;

int main() {
	int t,a,b,c,d,e,x;
	cin>>t;
	while(t--)
	{
	    cin>>a>>b>>c>>d>>e;
	    x=a+b;
	    if(a<=e || b<=e || c<=e){
                if(a+b <= d && c<=e)cout << "yes"<<endl;
                else if(b+c<=d && a<=e)cout << "yes"<<endl;
                else if(a+c<=d && b<=e)cout << "yes"<<endl;
                else cout << "no"<<endl;
            }
	    else{
	        cout<<"no"<<endl;
	    }
	}
	return 0;
}
