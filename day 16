#include <iostream>
using namespace std;

int main() {
	int t,temp=0;
	cin>>t;
	while(t--)
	{   int N;
	    cin>>N;
	   int arr[N];
	   for(int i=0;i<N;i++){
	       cin>>arr[i];
	   if(arr[i]>temp)
	   {
	       temp=arr[i];
	   }
	}
	cout<<temp;}
	return 0;
}
